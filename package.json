{
  "name": "removed-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx tsx find-asset-urls.mts",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
